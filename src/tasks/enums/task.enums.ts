export enum TaskPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum TaskStatus {
  Pending = 'pending',
  InProgress = 'in-progress',
  Completed = 'completed',
}

export enum TaskCategory {
  Work = 'work',
  Personal = 'personal',
  Shopping = 'shopping',
  Health = 'health',
  Other = 'other',
}
